spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dsa_mastery?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: dsa_user
    password: dsapassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

jwt:
  secret: dsamasteryhubsecretkeythatshouldbelonger
  expiration: 86400000

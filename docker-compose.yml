version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: dsa_mastery_mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: dsa_mastery
      MYSQL_USER: dsa_user
      MYSQL_PASSWORD: dsapassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always

volumes:
  mysql_data:
